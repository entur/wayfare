name: reisefrihet mobile

on:
  pull_request:
    paths:
      - "bffs/reisefrihet-fastify/**"
      - "clients/react-native/**"
      - ".github/workflows/reisefrihet-mobile.yml"
  push:
    branches: [main]
    paths:
      - "bffs/reisefrihet-fastify/**"
      - "clients/react-native/**"
      - ".github/workflows/reisefrihet-mobile.yml"

permissions:
  contents: read

jobs:
  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/dependency-review-action@v4

  bff:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: bffs/reisefrihet-fastify
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.1
      - uses: actions/setup-node@v6
        with:
          node-version: 24.15.0
          cache: pnpm
          cache-dependency-path: bffs/reisefrihet-fastify/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      # src/generated/reisefrihet.ts is checked against the live staging spec by
      # reisefrihet-drift-checks.yml on a schedule, not gated here, since that
      # spec can drift independently of any PR in this repo.
      - run: pnpm openapi:generate
      - run: git diff --exit-code -- contract/mobile.openapi.json

  mobile:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: clients/react-native
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.1
      - uses: actions/setup-node@v6
        with:
          node-version: 24.15.0
          cache: pnpm
          cache-dependency-path: clients/react-native/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm api:generate
      - run: git diff --exit-code -- src/api/schema.ts
      # expo-doctor's "expected" versions are fetched live from Expo's SDK bundle
      # and can drift within the same day, independent of any change in this repo.
      # Non-blocking here; reisefrihet-drift-checks.yml tracks it on a schedule.
      - run: pnpm run doctor
        continue-on-error: true
      - run: pnpm check
